<Window x:Class="RAM.UI.Dialogs.ConfirmDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="Confirm"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        WindowStartupLocation="CenterOwner"
        ShowInTaskbar="False"
        ResizeMode="NoResize"
        SizeToContent="Height"
        Width="440"
        TextElement.FontFamily="{DynamicResource FontSans}"
        TextElement.FontSize="{DynamicResource Fs13}">

    <Border Background="{DynamicResource BgElevated}"
            BorderBrush="{DynamicResource BorderDefault}"
            BorderThickness="1"
            CornerRadius="{DynamicResource RadiusLg}"
            Effect="{DynamicResource ShadowModal}"
            Padding="24"
            Margin="32">
        <StackPanel>
            <TextBlock x:Name="TitleText"
                       Text="Confirm"
                       FontSize="16"
                       FontWeight="SemiBold"
                       Foreground="{DynamicResource FgPrimary}"
                       Margin="0,0,0,8" />
            <TextBlock x:Name="MessageText"
                       Text="Are you sure?"
                       FontSize="13"
                       Foreground="{DynamicResource FgSecondary}"
                       TextWrapping="Wrap"
                       Margin="0,0,0,20" />

            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                <Button x:Name="CancelButton"
                        Style="{StaticResource BtnStyle}"
                        Click="OnCancel"
                        IsCancel="True"
                        Content="Cancel"
                        MinWidth="84" />
                <Button x:Name="ConfirmButton"
                        Click="OnConfirm"
                        IsDefault="True"
                        Margin="8,0,0,0"
                        MinWidth="84">
                    <Button.Style>
                        <Style TargetType="Button" BasedOn="{StaticResource BtnPrimaryStyle}">
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding IsDestructive, RelativeSource={RelativeSource AncestorType=Window}}"
                                             Value="True">
                                    <Setter Property="Background" Value="{DynamicResource StatusError}" />
                                    <Setter Property="BorderBrush" Value="{DynamicResource StatusError}" />
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </Button.Style>
                    <TextBlock x:Name="ConfirmText" Text="Confirm" />
                </Button>
            </StackPanel>
        </StackPanel>
    </Border>
</Window>
